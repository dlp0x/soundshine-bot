name: Sync Labels

on:
  push:
    paths:
      - '.github/labels.json'
  workflow_dispatch: 

jobs:
  labels:
    name: Update Labels
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Label Manager
        uses: lannonbr/issue-label-manager-action@4.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.TOKEN }}
